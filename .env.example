# --- Founder/Admin Settings ---
# These settings are for the project owner to bypass the public proxy 
# or use their own key for development and testing.
# Regular users of the `kylo` CLI do not need these.

# To force the CLI to use a direct Gemini connection instead of the proxy:
# KYLO_FORCE_GEMINI=true

# The personal Gemini API key to use when KYLO_FORCE_GEMINI is true:
# GEMINI_API_KEY=a_personal_gemini_api_key_for_direct_calls

# To point the CLI to a local or staging proxy server instead of the production one:
# KYLO_PROXY_URL=http://127.0.0.1:8080


# --- General Local CLI Behavior ---
KYLO_ENV=development  # development, production
KYLO_LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
KYLO_RATE_LIMIT_AUDITS=100  # audits per hour
KYLO_RATE_LIMIT_SECURE=50   # secure scans per hour
KYLO_CLI_PRIMARY_COLOR=magenta
KYLO_CLI_ACCENT_COLOR=purple

